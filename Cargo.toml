[package]
name = "memerunner"
version = "0.1.0"
authors = ["fominok"]

[dependencies]

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["winuser", "processthreadsapi", "memoryapi"] }
